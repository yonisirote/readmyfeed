{
  "name": "readmyfeed",
  "private": true,
  "workspaces": [
    "backup/server"
  ],
  "scripts": {
    "dev": "npm run dev -w backup/server",
    "smoke": "npm run smoke -w backup/server",
    "smoke:pagination:mock": "npm run smoke:pagination:mock -w backup/server"
  }
}
