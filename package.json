{
  "name": "readmyfeed",
  "private": true,
  "scripts": {
    "dev": "npm --prefix client run start",
    "android": "npm --prefix client run android",
    "ios": "npm --prefix client run ios",
    "web": "npm --prefix client run web",
    "fetch:cli": "npm --prefix client run fetch:cli"
  }
}
